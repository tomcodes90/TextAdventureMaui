
 <ContentView
    x:Class="TextAdventureMaui.Views.Controls.MenuButton"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Border x:Name="MainBorder"
            BackgroundColor="Transparent"
            StrokeThickness="0"
            StrokeShape="RoundRectangle 8">
        <Grid ColumnDefinitions="50,*" Padding="12" ColumnSpacing="3">
            <Image x:Name="IconImage" Grid.Column="0" HeightRequest="36" WidthRequest="36"/>
            <Label x:Name="TextLabel" Grid.Column="1" VerticalOptions="Center" FontSize="64" CharacterSpacing="2"/>
        </Grid>
    </Border>
</ContentView>
