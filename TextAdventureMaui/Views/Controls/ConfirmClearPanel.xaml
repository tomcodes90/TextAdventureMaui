<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TextAdventureMaui.ViewModels"
             x:Class="TextAdventureMaui.Views.Controls.ConfirmClearPanel"
             x:DataType="vm:BattleViewModel">

    <HorizontalStackLayout HorizontalOptions="Center" VerticalOptions="End" Spacing="10" Margin="0,10">
        <Button Text="Confirm" Command="{Binding ConfirmInputCommand}" 
                IsEnabled="{Binding IsEnemyTurn, Converter={StaticResource InverseBoolConverter}}" />
        <Button Text="Clear" Command="{Binding ClearInputCommand}" 
                IsEnabled="{Binding IsEnemyTurn, Converter={StaticResource InverseBoolConverter}}" />
    </HorizontalStackLayout>
</ContentView>